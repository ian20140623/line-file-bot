services:
  bot:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - TZ=Asia/Taipei
    volumes:
      - ./downloaded_files:/app/downloaded_files
      - ./generated_ics:/app/generated_ics
    restart: unless-stopped
